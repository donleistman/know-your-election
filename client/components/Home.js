import React from 'react';
import { Game, HeaderBar, MessageBar } from '.';

const Home = () => {
  return (
    <div>
      <HeaderBar />
      <MessageBar />
      <Game />
    </div>
  );
};

export default Home;
